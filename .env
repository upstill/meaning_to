# All credentials are now injected at build time from Vercel environment variables
# SUPABASE_URL - injected via --dart-define
# SUPABASE_ANON_KEY - injected via --dart-define
# YOUTUBE_API_KEY - injected via --dart-define
